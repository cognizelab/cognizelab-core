function out = exampleFunc(in)
%LABCOMMON.EXAMPLEFUNC Placeholder showing the +labcommon package convention.
%   OUT = LABCOMMON.EXAMPLEFUNC(IN) just echoes IN back.
%
%   Because this file lives in a "+labcommon" folder, call it as:
%       out = labcommon.exampleFunc(in)
%   after adding the parent "matlab/" folder to your MATLAB path.
%
%   Replace this with real shared functions (e.g. ROI extraction helpers,
%   first-level model builders, QC plotting) as you write them.

out = in;
end

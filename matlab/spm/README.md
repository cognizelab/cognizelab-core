# spm/

SPM-specific helper scripts and batch templates shared across projects
(e.g. first-level batch templates, contrast-definition helpers, QC scripts).
Keep these generic — project-specific onsets/contrasts belong in the
project's own repo.

function out = exampleFunc(in)
%COGNIZELABCORE.EXAMPLEFUNC Placeholder showing the +cognizelabcore package convention.
%   OUT = COGNIZELABCORE.EXAMPLEFUNC(IN) just echoes IN back.
%
%   Because this file lives in a "+cognizelabcore" folder, call it as:
%       out = cognizelabcore.exampleFunc(in)
%   after adding the parent "matlab/" folder to your MATLAB path.
%
%   Replace this with real shared functions (e.g. ROI extraction helpers,
%   first-level model builders, QC plotting) as you write them.

out = in;
end
